import Loader from "../components/common/Loader";

const LandingPage = () => {
  return (
    <div className="text-2xl">
      <Loader>This is the landing page</Loader>
    </div>
  );
};

export default LandingPage;
